name: holy_intensive
description: "App de leitura da Bíblia estilo Duolingo"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0

  # HTTP para API da Bíblia (IMPORTANTE pra próxima fase)
  http: ^1.2.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true